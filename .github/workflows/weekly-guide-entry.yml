name: Weekly Hocking Hills Local Guide Entry Generator

# Runs every Monday at 9:00 AM UTC and can also be triggered manually.
on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  generate-guide-entry:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # ── Generate the guide entry ────────────────────────────────────────────
      - name: Generate bilingual guide entry
        id: generate
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: node .github/scripts/generate-guide-entry.mjs

      # ── Commit & push the new file to a fresh branch ───────────────────────
      - name: Commit guide entry and push branch
        id: commit
        run: |
          DATE=$(date +%Y-%m-%d)
          BRANCH="guide/weekly-${DATE}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Check whether a new file was actually created (new files are untracked)
          if [ -z "$(git status --porcelain -- src/content/guide/)" ]; then
            echo "No new guide entry was generated — nothing to commit."
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git checkout -b "$BRANCH"
          git add src/content/guide/
          git commit -m "content: add weekly Hocking Hills local guide entry (${DATE})"
          git push origin "$BRANCH"

          echo "has_changes=true"  >> "$GITHUB_OUTPUT"
          echo "branch=${BRANCH}"  >> "$GITHUB_OUTPUT"
          echo "date=${DATE}"      >> "$GITHUB_OUTPUT"

      # ── Open a pull request ─────────────────────────────────────────────────
      - name: Create pull request
        if: steps.commit.outputs.has_changes == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --title "🌳 Weekly Local Guide Entry: Hocking Hills (${{ steps.commit.outputs.date }})" \
            --body "$(cat <<'PREOF'
          ## Weekly Auto-Generated Local Guide Entry

          A new bilingual (English + Spanish) local guide entry about Hocking Hills has been generated and is ready for your review.

          ### Review checklist

          - [ ] Topic is unique — not a duplicate of existing content
          - [ ] English content is accurate, well-written, and family-friendly
          - [ ] Spanish translation reads naturally (not a literal word-for-word translation)
          - [ ] Tags are relevant and appropriate
          - [ ] No sensitive, incorrect, or outdated information

          ### Publishing

          Merge this PR to publish the entry on the website. The deploy workflow will build and push the updated site automatically.

          > **Note:** This PR was created automatically by the weekly local guide workflow. Please review carefully before merging.
          PREOF
          )" \
            --base main \
            --head "${{ steps.commit.outputs.branch }}"
